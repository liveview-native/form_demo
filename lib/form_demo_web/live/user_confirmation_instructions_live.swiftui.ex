defmodule FormDemoWeb.UserConfirmationInstructionsLive.SwiftUI do
  use FormDemoNative, [:render_component, format: :swiftui]

  import FormDemoWeb.CoreComponents.SwiftUI

  def render(assigns, _) do
    ~LVN"""
    todo
    """
  end
end
